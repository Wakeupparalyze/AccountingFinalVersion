<Page x:Class="Accounting.RegistrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Accounting"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RegistrationPage">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label HorizontalAlignment="Center" Content="Регистрация"/>
            <Label Content="Фамилия"/>
            <TextBox Text="{Binding LastName}"/>
            <Label Content="Имя"/>
            <TextBox Text="{Binding FirstName}"/>
            <Label Content="Отчество"/>
            <TextBox Text="{Binding Patronymic}"/>
            <Label Content="Логин"/>
            <TextBox MinWidth="200" Text="{Binding Login}"/>
            <Label Content="Пароль"/>
            <TextBox Text="{Binding Password}"/>
            <Button Margin="0 5 0 0" Content="Зарегестрироваться" Click="Registration"/>
            <Button Margin="0 5 0 0" Content="Назад" Click="ToSignInPage"/>
        </StackPanel>
    </Grid>
</Page>
